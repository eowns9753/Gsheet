using System.Collections.Generic;
using System.Threading.Tasks;
using SheetData.IO;

namespace SheetData.Generator
{
    public class SheetData
    {
        /*public static Dictionary<string, Localize> Localize;
        public static ExampleClass[] ExampleClass;
        public static Dictionary<string, ExampleSturct> ExampleSturct;

        public static async Task LoadAsync()
        {
            SheetBinaryReader reader = await SheetBinaryReader.Create("sheetData.bin");
            reader.ReadRef(out Localize);
            reader.ReadRef(out ExampleClass);
            reader.Dispose();
        }

        public static async Task SaveAsync()
        {
            SheetBinaryWriter writer = await SheetBinaryWriter.Create("sheetData.bin");
            writer.WriteRef(Localize);
            writer.WriteRef(ExampleClass);
            writer.Save();
            writer.Dispose();
        }*/
    }
}